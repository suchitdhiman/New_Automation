<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>TestNG-P010-TestNg</groupId>
	<artifactId>TestNG-P010-TestNg</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>TestNG-P010-TestNg</name>

	<properties>
		<maven.compiler.release>17</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<!-- Which suite `mvn test` runs. Override: -DsuiteXmlFile=suites/parallel-methods.xml -->
		<suiteXmlFile>testng.xml</suiteXmlFile>
		<!-- -Dheadless=true for CI -->
		<headless>false</headless>

		<testng.version>7.10.2</testng.version>
		<selenium.version>4.47.0</selenium.version>
		<log4j.version>1.2.17</log4j.version>
		<extentreports.version>4.1.7</extentreports.version>
		<surefire.version>3.5.2</surefire.version>
		<compiler.plugin.version>3.13.0</compiler.plugin.version>
	</properties>

	<dependencies>
		<!-- https://mvnrepository.com/artifact/org.testng/testng -->
		<dependency>
			<groupId>org.testng</groupId>
			<artifactId>testng</artifactId>
			<version>${testng.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.seleniumhq.selenium/selenium-java -->
		<dependency>
			<groupId>org.seleniumhq.selenium</groupId>
			<artifactId>selenium-java</artifactId>
			<version>${selenium.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- https://mvnrepository.com/artifact/log4j/log4j -->
		<dependency>
			<groupId>log4j</groupId>
			<artifactId>log4j</artifactId>
			<version>${log4j.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- https://mvnrepository.com/artifact/com.aventstack/extentreports -->
		<dependency>
			<groupId>com.aventstack</groupId>
			<artifactId>extentreports</artifactId>
			<version>${extentreports.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${compiler.plugin.version}</version>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${surefire.version}</version>
				<configuration>
					<suiteXmlFiles>
						<suiteXmlFile>${suiteXmlFile}</suiteXmlFile>
					</suiteXmlFiles>
					<!-- Parallelism is declared in the suite xml (parallel="..." /
					     thread-count / data-provider-thread-count), so surefire's
					     own <parallel> is deliberately left unset. -->
					<systemPropertyVariables>
						<headless>${headless}</headless>
					</systemPropertyVariables>
					<!-- Report the failure instead of aborting the whole build. -->
					<testFailureIgnore>false</testFailureIgnore>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
